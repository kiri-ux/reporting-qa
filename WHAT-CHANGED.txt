build 2026.09.11-234
Changed files only. Everything since 130 - install this one.
37 checks.

EVERY NAME A CTV PRODUCT PRINTS, written down from the orders rather than from
the reports that happened to be to hand. The word OTT appeared nowhere in the
code. These were all read as no product at all:

  Prime OTT, Amazon OTT, Behavioral OTT      line items
  OTT Amazon, CTV Amazon                     line items
  Connected TV                               line items
  Social Mirror OTT                          line items
  YouTube TV                                 line items
  OTT / Amazon CTV / Prime OTT Performance   widget titles

Which compounds: a report whose CTV prints as OTT carried no CTV product, so
the page-one tile had no CTV rows to be measured against, so the check said
nothing at all about it.

YouTube TV rows are CTV inventory for the tile and the product stays YouTube -
the order is YouTube+, not a CTV buy. The CTV Run reads YouTube reports for
that reason: a report whose only CTV is YouTube TV carries no product with CTV
in its name.

NOTHING TO COMPARE IT TO IS NOT THE SAME AS NOTHING WRONG. The tile check
returned an empty list when it found no CTV rows, which reads on the report as
a pass. Where the client has a CTV order and the report prints the tile, that
is now a finding: "CTV completion rate could not be checked". Either the grid
is missing or it is titled something the tool has not been taught. With no CTV
order it stays quiet - a tile with no rows is "a CTV tile on a report with no
CTV", which is a check of its own.

THE CTV TILE CHECK NEVER RAN ON A SINGLE REPORT before this. It wanted "CTV
Completion Rate" alone on a line, and page one puts three tiles side by side,
so pdftotext prints the three headings as one line. It reads the tile down its
own column now: 98.60% on Watsontown, 98.90% on Central Penn, both inside their
own CTV rows, both correctly silent.

RUN ONE CHECK NOW, over only the reports it is about. A Run button on every row
of the flags tab. Only checks that read the inside of a product's own widget
are scoped by product, matched against what was DETECTED on the report:

  CTV tile                       CTV, Social Mirror CTV, YouTube
  Social Mirror ad-size naming   Social Mirror
  Social Mirror preview shape    Social Mirror
  Geo-fencing row names          Mobile Conquesting

Everything else reads the whole cycle, which is slower and never wrong. A check
about a product being ABSENT is never scoped.

A row with a run going stays on screen. "Flagging now" hides the checks with no
count, which is every check that has just been fixed.

972 tests pass.
