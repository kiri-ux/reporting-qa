build 2026.09.10-225
Changed files only. Everything since 130 - install this one.
36 checks.

The filter row was gone. A block I added in 224 went into the card filters as
well as the report ones, where the variable it reads does not exist, so the
script threw before either row was built. Both are back.

Filter the reports by which finding. "Errors" is three hundred reports and four
different problems, and answering which of them meant opening rows until you
found the right kind. The menu is built over the whole cycle and prints what
the Findings column already says.

Every status can be picked whether or not one is on the page, and a server-side
filter with one option is offered - it narrows the whole cycle, not the fifty
rows on screen.

942 tests pass.
