build 2026.09.11-232
Changed files only. Everything since 130 - install this one.
37 checks.

THE CTV TILE CHECK NEVER RAN ON A SINGLE REPORT. It wanted "CTV Completion
Rate" alone on a line, and page one puts three tiles side by side, so
pdftotext prints the three headings as one line. It stood down on every report
on the board, and a skipped check with a dash beside it reads exactly like a
check that is working and finding nothing.

It reads the tile down its own column now - the tile to its left is the product
breakout, which is nothing but percentages. Checked against the real files:
98.60% on Watsontown, 98.90% on Central Penn, both inside their own CTV rows,
both correctly silent.

RUN ONE CHECK NOW, over only the reports it is about. A Run button on every row
of the flags tab. A rule about the CTV tile has nothing to say about a report
with no CTV on it, so it reads the reports carrying the product - about a
hundred instead of twelve hundred. It is a full re-check of those reports, not
a run of that one rule: the cost is reading the PDF either way, and
half-judging a report is how one ends up carrying two builds' answers.

A row with a run going stays on screen. "Flagging now" hides the checks with no
count, which is every check that has just been fixed - the one you came to
press Run on.

966 tests pass.
