build 2026.08.25-40
Changed files only. Drop these over your copy, keeping the paths.

app/version.py
app/main.py
app/checks/rules.py
app/templates/base.html
app/templates/cycle.html
app/templates/links.html        (new)
tests/test_manual_upload.py
tests/test_quality.py


1. Client links, on their own page
------------------------------------
/cycle/links?period=2026-07. Every packaged partner, with Open, Copy, and the
raw URL in view. The cycle board no longer stacks them above the work - the
Partners delivered tile links across, and each partner's own card keeps its
Client link where it sits in context.

Package & share now lands you on that page rather than back on the board. It
is the last step before sending the link to somebody, and going back to 146
cards with the new one somewhere in them was a find-it-yourself puzzle at
exactly the wrong moment.

The one just packaged goes first, whatever the sort, with a gold border and a
JUST PACKAGED tag. The mark comes off when you copy it, open it, or reload -
?new= is taken out of the address bar as soon as the page has drawn, so a
refresh loads a page with nothing marked. A failed package still goes back to
the board, where the reason and the Try again button are.


2. Window World, again - and why the fix did not take
-------------------------------------------------------
You are right that it should have gone. The parser fix in build 39 handles the
case I could reconstruct; this report is evidently shaped differently, and I
cannot see it without the file.

What I can fix without the file is the part that was indefensible either way.
The check was reporting "0 over 0 is nothing left to divide" and failing the
report on it. But a tile that filtered out EVERY line item would print nothing,
and it printed 0.45% - so whatever that classification is, it is not what the
tile did. Failing a correct report on reasoning that is visibly broken the
moment you open Investigate is the worst of both.

Both checks now abstain in that case:

  - the CTR check says "could not be checked against its own numbers" (info,
    not a failure)
  - the clicks check says "could not be checked against the line items" (info)

and both print the line item names AS THE PARSER READ THEM in Investigate.
That is the part that is wrong, and it is invisible from the numbers - the
report looks fine and the finding looks reasoned.

If you open Window World after this and send me that "Names as read" list, I
can fix the parse properly rather than guessing at the layout. Or send me the
PDF and I will do it from the file.


Tests: 308 passing.
