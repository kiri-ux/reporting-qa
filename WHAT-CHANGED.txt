build 2026.09.08-221
Changed files only. Everything since 130 - install this one.
35 checks.

A spend product is read off its own tiles, and the tile line is not held to the
eighty-character cap on widget titles. Two tiles side by side are one line of
text with the width of the page between them: "PPC Ad Cost ... PPC
Cost-Per-Click" is 95 characters and Peters - Troy's is 107, so 220 read
neither. The test fixture was padded to fit under the cap, which is why it
passed while the report failed.

The same client spelled two ways is one report, not two.

A row shows the copy that was signed off, then the newest, and says "+1 file"
when another report this cycle names the same client.

A completion grid is not more rows of the grid above it.

A row is read one number per column, wherever the table narrows.

A replacement takes the new file's logo.

935 tests pass.
