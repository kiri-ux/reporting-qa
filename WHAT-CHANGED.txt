build 2026.09.01-134
Changed files only. Includes 131, 132 and 133 - install this one, not all four.

app/audit.py
app/db.py
app/main.py
app/orders_io.py
app/partners.py
app/templates/audit.html
app/templates/base.html
app/templates/why_slow.html      (new)
app/timing.py                    (new)
app/version.py
tests/test_timing.py             (new)


THE VSCU ROWS - YOU WERE RIGHT, AND THE TOOL WAS LYING ABOUT WHY
================================================================
I loaded your Aug 31 export and ran those four order numbers through it.

  52263 VSCU KC       Online Audio   Aug 31 -> Sep 13   IO Live
  52265 VSCU SC       same
  52267 VSCU SJC      same
  52268 VSCU TC KC    same

One day in August. The 31st. The board's rule is that a campaign running fewer
than seven days in a month is not owed a monthly - a page of near-zero numbers
that reads as a broken report - so it left all four off, correctly, and it knew
exactly why.

The list check then threw that reason away and printed the catch-all: "the
order is loaded and looks live, worth a closer look." It sent you to check four
order screens for something the tool already knew.

TWO FAULTS, BOTH MINE.

Your tracker writes "ADM - VSCU KC" and the code that strips the market code
off the front wanted TWO words - it was written against "7MOU SG - Benton
Rodeo" and never saw a one-word code. So the client came out as "ADM - VSCU KC"
instead of "VSCU KC", matched no board row by name, and fell past the real
answer. (Also made case sensitive now, so it cannot eat a real client name like
"Bliss - Digital Innovations".)

And it only ever looked the board's reason up BY NAME - on a page whose entire
purpose is that the two tools spell clients differently. It has the order id,
which is the one thing they agree on, so it now asks again under whatever name
the board uses. Even if a name never lines up, the reason comes through.

Those four rows now read: ran 1 day this month. Nothing to do about them.

AND THE ONES THAT ARE ACTUALLY WORTH YOUR TIME ARE TAGGED. The catch-all is the
only "I do not know" on that page and it looked identical to the forty answers
that are the rules working. It carries a "look" tag now. Everything untagged
has a real reason behind it.


YOUR OTHER QUESTION: YES, THE ROTO ROOTER APPROVE WORKED
========================================================
I built your case rather than reading the code and telling you what it looked
like it did. Order 52290, live all year, taken off August for not being in the
serving file. Before the approve, no row. After it, the August monthly is on
the board tagged kept on by hand, with "client wasn't linked up" and your
initial against it. August only - September asks again.

AND ONE THING I FOUND WHILE CHECKING, WHICH IS WORSE THAN IT LOOKS. Rejecting a
row you had already approved did not take it back off. The word on the list
changed to Rejected and the override stayed put, so the board still carried the
row somebody had just rejected. Two screens, two answers, and the one nobody
was looking at was the one the reporters work from. Clearing had the same hole.
Both reach the board now, and only that one client's - a sweep that cleared the
cycle's overrides would be a very quiet way to lose somebody else's work.


LIVE CHAT - NEITHER
===================
Not filtered. 26734 is not in your Aug 31 export at all; Live Chat generally is
(4,218 lines, 170 orders). But the message was wrong: "not in the export" is
what it says when it cannot find the order AT ALL, so a Live Chat only client
got that sentence with the line sitting right there. It now says "Live Chat
does not earn a report on its own", which is your rule working.


THE TWO YOU ASKED FOR
=====================
The wall of 137 partner names is folded into "Which partners".

Check a list carries the cycle in its title, and says under it that the list
and every approve and reject on it apply to that cycle only.


THE SLOWNESS
============
Memory ruled out (196 MB of 2,048 - I had 512 from a stale comment). Restarts
ruled out (two boots in 24 hours, both mine). What is left is load 7.1/7.4/7.5
with the fifteen-minute figure highest, and `/` at 3.9 seconds with 0.18 of a
second of it in the database. Not the database - a box taking turns with six
other things, and inside a container that load figure is usually the whole host.

Slow pages (over 3 seconds) now write themselves down from both workers, and
every page is timed in two halves - working it out against drawing it. Bottom
of "Why is it slow" in the rail. You do not have to catch one happening.

Three things got cheaper: the board stopped re-working out which first names
clash (1,964 times a page, a third of its build), the list check stopped
reading the whole order table twice per row (300 passes for a list of 150), and
an upload over 8 MB streams from disk instead of holding three copies in memory
(1,046 MB down to 233 on your real file).

763 tests pass.
