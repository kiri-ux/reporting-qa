build 2026.08.28-108
Changed files only.

app/templates/cycle.html
app/templates/viewer.html
app/version.py
tests/test_board_extras.py


"THE ORDER DOES NOT SAY" - WHAT IT WAS
=========================================
The order pills are coloured off the STATUS on that order's line items in the
export: green live, orange paused, red cancelled, blue complete. Grey means
none of the line items behind that order carried a status at all, so there was
nothing to colour it with.

That is all it ever meant. But "the order does not say" reads as the tool being
coy about something it knows, or as the order itself being odd, when the plain
fact is that the export's status column came through empty for order 51903. It
now says:

  Order 51903 - no status on it in the order export. Opens in the IO tool.

Nothing about the pill changed - the wording did. If you open 51903 in the IO
tool and it plainly says IO Live, then the export is the thing to look at, not
the order.


NEEDS FIX DOES NOT SEND ANYTHING
===================================
You are right, and it was the tooltip that was wrong rather than the button.

  Needs fix - send it back to be re-pulled

promised a re-pull. Pressing it does exactly two things: it marks the row, and
it keeps that report out of the partner's folder until somebody deals with it.
Nothing is sent, nothing is requested, and no re-pull happens on its own. Now:

  Needs fix - marks it as wrong and keeps it out of the partner's folder.
  Re-pulling it is still on you.

The same button on the report viewer had no tooltip at all and now carries the
same sentence.


VIEW ORDER LINES
===================
Was a sentence with a link buried in it, in small grey print, under a panel of
more small grey print. It is a button now, in the same style as "Check this
file again", and it still opens in the side sheet over the report rather than
taking you off the page. Middle-click still gives you the full page.

671 tests pass.
