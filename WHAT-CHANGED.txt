build 2026.09.08-212
Changed files only. Everything since 130 - install this one.
35 checks.

Corrected reports now reach Drive. The Drive upload recorded the name it filed
under and never the file - and a corrected report keeps its name, only the file
underneath changes. So sync compared a name to itself, decided nothing had
moved, and skipped it. Dropbox had always recorded both.

Nothing goes out unstamped. Every report filed to Drive has no record of which
file went up, so the first sync of each partner after this deploy sends the
month in full and writes it down. After that it sends only what moved.

PowerPoint uploads. Some SEO comes back as a deck: stored, named, signed off
and packaged like anything else, checks not run - there is nothing in a deck
for a rule about a Digital Marketing Report widget to read. The report page
offers it for download instead of embedding it.

917 tests pass.
