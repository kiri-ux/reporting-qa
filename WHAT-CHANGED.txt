build 2026.08.28-109
Changed files only.

app/board.py
app/templates/cycle.html
app/templates/viewer.html
app/version.py
tests/test_board_extras.py


YOU WERE RIGHT. THE EXPORT HAS THE STATUS.
=============================================
I checked the four order files you sent: 1,404 rows, and not one of them has a
blank status. Both columns are filled on every row, so "no status on it in the
order export" was wrong about your export, and build 108 was wrong to say it.

The board was not reading the status at all.

The order lines come out of the database column by column - market, client,
order ids, dates, product, and so on - a named list, for speed. The line items
underneath were not on that list. The code that colours the pills reads them,
found nothing every single time, and carried on, because reading a field that
is not there is not an error in Python. It just comes back empty.

So EVERY order pill on the board has been grey since the colours shipped, on
every partner, and the tooltip has been blaming the export for it. Green,
orange, red and blue were never once drawn.

The line items are on the list now. The pills should come up coloured on the
next load - no re-sync, no re-check, nothing to press.

There was a second, smaller thing behind it, and it is fixed too: an order that
has finished but overlaps a campaign still gets its id onto that row, because
the row is about the whole campaign. The status was only being read for orders
that earn a row of their own, so those particular pills would have stayed grey
even with the field present. They are read for every order on the client now.

If a pill is still grey after this, the export really is missing that status,
and the tooltip says so.


NEEDS FIX
============
Your wording:

  Needs fix - marks it as wrong and keeps it out of the partner's folder.
  It needs to be re-pulled and uploaded.

On the board's icon and on the viewer's button.

672 tests pass.
