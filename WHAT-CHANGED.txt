build 2026.09.10-228
Changed files only. Everything since 130 - install this one.
36 checks.

Switch several checks at once. A tick box on every row and two buttons at the
top of the flags tab - switch off ticked, switch on ticked. The row's own On/Off
still works on its own.

And it stays where you were. Switching one used to reload the page onto the
other tab, at the top, so switching four meant finding the tab again three
times. The tab is in the URL now, and in the sheet the scroll position and the
All / Flagging now choice come back too.

THE SWEEP COVERS THE CYCLE BEING WORKED, not three cycles. That is where
"2,594 reports still to go" came from on a board of 1,275 - two thirds of the
queue was months that had already shipped. Older cycles are re-checked from
their own board, by pressing the button.

To be clear about what switching a check off does: it is off everywhere. Not
run on a new upload, not run on anything the Dropbox feed brings in, not run on
a re-check, and anything it already flagged stops counting on reports already
on the board. Nothing is re-read for that.

Switching one back ON does queue a re-check - that rule never ran on anything
judged while it was off.

952 tests pass.
