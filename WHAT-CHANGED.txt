build 2026.09.01-153
Changed files only. Everything since 130 - install this one.

app/audit.py
app/board.py
app/db.py
app/main.py
app/orders_io.py
app/orders_s3.py
app/partners.py
app/templates/audit.html
app/templates/base.html
app/templates/cycle.html
app/templates/orders.html
app/templates/report_orders_body.html
app/templates/why_slow.html      (new)
app/timing.py                    (new)
app/version.py
tests/test_board_extras.py
tests/test_timing.py             (new)


THE ROGUE COMMA AND THE ROGUE SEMICOLON ARE THE SAME BUG
========================================================
That bar is laid out with flex, so the spinner and the button can sit beside
the text. Flex makes a flex item of EVERY child - including every bare run of
text and every <code> - and each one wraps on its own. So the sentence broke
after the bold heading and left the comma alone at the start of the next line,
and the semicolon floated between two code spans.

The prose is one element now, so it wraps like a sentence. And the stale-code
panel is rewritten while I was in there: it was a sentence with two hashes
buried in the middle of it, which nobody reads as a sentence.

  These rows were written by older import code. They were last read Sep 1 at
  2:57 PM, and the code that reads the export has changed since. The product
  check abstains until they are read again.
    then 36566e3b05423be7:2026-08 · now 36566e3b05423be7

The two fingerprints are still there, on their own line, where they are
reference rather than grammar.


NO - IT IS A SUM OF REAL MONTHS, NEVER A MULTIPLICATION
=======================================================
You are right to ask, and multiplying would be making a number up. It does not
do that and it never will.

WHAT IT DOES: the monthly impressions figure in the export is real. Where a
line item has rows for several months, those actual values are added together.
That is the whole of it - no rate, no extrapolation, no month count.

WHERE THE TOTAL WOULD SHOW: the Campaign total column, under the money that is
already there.

AND WHY IT IS EMPTY ON YOUR DATA, which is the real answer to your question.
Of the 10,597 line items in the export I have, NOT ONE has more than a single
dated row - every line item's date is pinned to the month it started. There are
no months to add up, so there is nothing to show, so it shows nothing. It does
not fill the gap with a guess.

THE COLUMN THAT CLAIMS TO HAVE IT DOES NOT. total_campaign_impressions appears
four times in the header; three are empty and the populated one holds "1" - a
share of goal, not a count. There has been a guard throwing that away since
before today, which is why Campaign total has only ever shown money.

So: the figure is not in this export, in either form. If it exists in TapClicks
itself or in a different export, point me at it and I will read it from there.
The summing code stays in and will fill the column on its own the day a file
arrives carrying per-month rows.

805 tests pass.
