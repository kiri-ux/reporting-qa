build 2026.09.14-256
Changed files only. Everything since 130 - install this one.
38 checks.

YOUR ORDERS ARE CURRENT NOW. The yellow "these rows were written by older
import code" box is gone from the order lines panel on 1623, which is the
thing that had check_products switched off. That part is fixed.

WHAT IS LEFT IS THE STORED ANSWER. Susquehanna's report was last judged on 3
September and findings are stored, so it is still showing what it said then.
Press "Check this file again" on that report and the geo-framing finding will
be there.

AND THE TWO THINGS THAT WERE SUPPOSED TO HAVE DONE THAT FOR YOU BOTH MISSED IT
FOR THE SAME REASON. Each matched on the sentence the tool writes when a check
stands down - and that sentence only exists on reports judged since the
sentence did. A 3 September answer has a skipped product check with no reason
recorded on it, so:

  - the re-read did not queue it
  - the report page did not say the product check had not run

Both key off the check being skipped now, whatever the reason. Which means the
one report the whole thing was written for was the one both filters hid.

So on this build the page says it without being asked:

  The product check did not run.
  This answer was stored before the reason was recorded. Check the file again.

1023 tests pass.
