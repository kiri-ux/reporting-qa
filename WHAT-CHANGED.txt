build 2026.09.11-239
Changed files only. Everything since 130 - install this one.
37 checks.

THE RESEND MARK WAS NEVER COMING OFF. It says a failure turned up on a file the
partner already has, and the board shows that as Report review ahead of
everything else - ahead of what the new file says. Nothing that puts a new file
on a report was clearing it, so 56 reports were repulled, arrived, and every
one of them went on saying Report review about the copy it had replaced.
Nothing on the board moved, which is what a resend looks like when it has not
worked.

Cleared now on all three paths: the feed, accepting a file that was held, and
uploading a correction by hand. The pulled-sign-off mark goes with it - the row
was saying "Paloma signed this off, then a re-check found a failure" about a
file Paloma has never seen.

AND THE ONES ALREADY STANDING ARE PUT RIGHT ON THE NEXT BOOT. A report moves to
the batch that corrected it, so a batch that arrived after the mark was set is
a file that arrived after the mark was set. Nothing to press.

A PICTURE THAT IS NOT A PREVIEW IS NOT CALLED A BANNER. Social Mirror Ad
Screenshots sits under the grid on the same page and its strip is 2205x464,
which is a banner shape by any measure - Midwest Technical Institute was
failed for it with three square previews above it. The check says nothing
unless the pictures and the rows line up one to one.

A VERDICT LANDS BACK ON THE ROW, not the top of the board, with a highlight
that fades. Signing off a report near the bottom of a page of fifty meant
scrolling down to it again, every time.

A RUN COUNTS WHAT IT WAS FOR. "339 read, 1 changed" counted reports whose
severity moved - a report already failing picks up this finding and goes on
failing. It says "339 read, 64 flagged" now, and the 64 is a link to those
reports on the board.

982 tests pass.
