build 2026.09.08-202
Changed files only. Everything since 130 - install this one.
35 checks.

THE UPLOAD THAT OPENED A DIFFERENT MARKET
=========================================
Real bug, and a bad one. When you upload a file, the tool first looks for an
existing report to replace. That lookup matched on ORDER ID ACROSS EVERY REPORT
IN THE CYCLE and never checked the market - so an upload made against one
partner's row landed on another partner's report the moment the two shared an
id. Which is exactly what you saw: a different market and a different campaign,
twice, including after a manual re-pull.

An upload is made from a row. It can only replace that row's report now.


NO-LOGO PARTNERS
================
Amazing Results LLC, Ampersand, Grace Impressions. Never flagged for the
default logo - they have never sent one, so there is nothing behind the
finding. Both spellings of Amazing are in, in case the export has mine. Send
more names and I will add them; they are one line in app/checks/rules.py.


THE BANNER STOPS REPEATING THE BUILD NOTE
=========================================
It named the build and then printed a sentence about the deploy on a banner
about a queue. The build number identifies the deploy; that is enough.


897 tests pass.
