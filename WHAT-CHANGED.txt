build 2026.09.10-224
Changed files only. Everything since 130 - install this one.
36 checks.

The sweep reads signed-off reports. It used to, then stopped - re-reading
finished work while a rule changes several times a day meant the queue never
emptied and the board kept un-reviewing itself. That was the wrong half to
leave out: signed off is what has GONE TO THE PARTNER, so a check added
afterward reached no report a client was holding. On a cycle of 1,222 with
1,206 signed off, the sweep covered 16.

A new failure on a delivered report now has somewhere to go - Report review -
instead of dropping back into the pile waiting to be read, which is what made
this bearable to turn back on. The banner number, the partner Re-check button
and Skip this re-check all cover the same population again.

Every status can be picked in the Status filter, whether or not one is on the
page. These filter on the server, so "no row here has it" is exactly when you
want to pick it - Report review could not be selected until a report already in
Report review happened to be on screen.

"Geo-Framing Display Ads" is not "Display Ads".

941 tests pass.
