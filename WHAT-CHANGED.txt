build 2026.09.10-230
Changed files only. Everything since 130 - install this one.
36 checks.

RE-CHECKS WERE NOT RUNNING. Two faults, both introduced in 227 with the
switches.

The two workers disagreed about what the current rules are. The rules hash was
cached once per process, which was safe while it was a pure function of the
source and stopped being safe the moment a check could be switched off: only
the worker that took the click recomputed. The other one kept the old hash,
stamped every report it re-checked with it, and the first worker went on
counting those same reports as behind. That is a count that sits still while
the sweep works. The hash is cached against the set of switched-off checks now,
and the switches are re-read every fifteen seconds, so both workers agree
within fifteen seconds of a change.

And a sweeper that threw wedged its worker for good. One flag says a sweeper is
already going, and it only came off on the paths that were written. The order
re-read runs before the sweep and is the heaviest thing in the file; one throw
there left the flag set with nothing behind it, and that worker never started
another sweeper. It comes off in a finally now.

A skipped check says WHICH of its reasons. The product check stands down for
three and printed all three at once, which does not answer the question. The
one that matters: changing how a product name is read means every product
finding goes quiet until the order export has been read again, so a fix ships,
the re-check runs, and the finding it was written for does not appear. The
report now says that in as many words.

--- also in this install ---

A Display order spends the geo-framing allowance. Geo-Framing may print as an
ordinary Display widget, but not while the client also has a plain Display
order - one widget was answering for both.

Switch several checks at once, and stay on the tab. The sweep covers the cycle
being worked rather than three cycles.

958 tests pass.
