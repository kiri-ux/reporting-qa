build 2026.09.11-251
Changed files only. Everything since 130 - install this one.
38 checks.

ONE CHECK FOR A WIDGET BELONGING TO A PRODUCT THIS BUY DOES NOT INCLUDE. The
Amazon Display one is folded into it and PPC is added, as one table - the next
one somebody spots is a line rather than a new check.

  Amazon Premium Display   an Amazon Premium CTV + Video buy with no Amazon
                           Display line item
  PPC                      PPC's own pages on a buy with no PPC on the report
                           and none on the order

Charlottesville's Earthly Cleaning bought Display and Performance Max and
carried four pages of PPC: "Cost: Amount Spent on the PPC campaign", the
sitelink and callout extension diagram, the click glossary. Performance Max is
charged per event - its own tiles on that report say Client CPE - so the
cost-per-click glossary is PPC's, and PPC is not on the buy. PPC line items are
named for the strategy rather than the product, so that one is read off the
report's products and the order instead of the line items.

The check is not scoped by product and cannot be: the scope matches what the
report was detected as running, and this is about a product that is not part of
the buy. Scoping it would skip every report it is about.

It abstains, and says so, on a report with no line item grid to read - "no such
line item" is true of every report that failed to parse, which is not an answer
about the buy.

Renegade Marine now flags an Amazon Premium Display widget too. Its line items
are Amazon Prime CTV and Amazon Video with no Amazon Display, so it is the same
thing Fisher Tire and Window World have.

--- also in this install ---

The product check had been switched off on every report on the board: the order
sync writes "<hash>:<period>" and four readers compared it to the bare hash.
One producer now, with a test that fails if a second appears. RE-READ THE
ORDERS ONCE AFTER INSTALLING, then run the checks.

A conversion breakout is no longer evidence that a product ran.

Each half of an Amazon Premium buy owes its own creative and completion
widgets.

Every pacing goal is cut to the days the product actually had.

1014 tests pass.
