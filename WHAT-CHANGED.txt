build 2026.09.11-235
Changed files only. Everything since 130 - install this one.
37 checks.

EVERY BUTTON IN THE RULES SHEET WAS DOING NOTHING, in silence. A browser sends
a submit button's own name and value only when it is the one clicked - which is
what lets Run, the row's On/Off and the two bulk buttons share one form. The
sheet posts the form itself, and the call it used does not include the button.
So the sheet posted a form saying nothing and the server did nothing with it.
It worked on the full page, where the browser submits for itself, which is why
it went unnoticed.

NOTHING RE-CHECKS ITSELF ANY MORE. It all lives on the Reporting flags page:

  Run all re-checks     everything on this cycle the current rules have not read
  Run                   one check, over the reports it can say anything about
  Stop                  ends a run at the end of the batch it is on

A run says how far through it is - "412 of 1,395" - and what it did when it
finishes: "96 read, 3 changed", or "nothing to read". A run that finished fast
used to leave no trace at all, which is indistinguishable from the button not
working.

The Hold switch is gone with the sweep it held.

THE ORDER RE-READ STAYS AUTOMATIC, deliberately. The order list is parsed once
and only the answer is kept, so a change to the import code leaves every loaded
order carrying the old reading - and the product checks stand down entirely
until it is re-read. Nobody would know to press that, and it is what hid the
Geo-Framing fix.

974 tests pass.
