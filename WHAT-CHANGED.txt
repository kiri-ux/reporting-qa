build 2026.09.01-150
Changed files only. Everything since 130 - install this one.

app/audit.py
app/board.py
app/db.py
app/main.py
app/orders_io.py
app/orders_s3.py
app/partners.py
app/templates/audit.html
app/templates/base.html
app/templates/cycle.html
app/templates/orders.html
app/templates/why_slow.html      (new)
app/timing.py                    (new)
app/version.py
tests/test_board_extras.py
tests/test_timing.py             (new)


53872 - NO, AND IT WAS DOING NOTHING SILENTLY
=============================================
You approved it, the page said APPROVED, and nothing was added. Here is the
whole of why, because it is worth knowing.

The approve writes a "needed" override, and that override could only ever KEEP
a row the rules were about to remove. It could not create one. So for every
client the export DOES have, approving worked - and for a client the export has
never heard of, it recorded your decision and changed nothing at all. Which is
exactly the case the button is for: 53872 is on that list PRECISELY because the
export does not have it.

It also never got as far as writing the override, because it looks the partner
up off the order lines and there are none, so it had no market and quietly
stopped. Two failures stacked, and neither said a word.

Both fixed. The market code from your tracker row - NAV, WHIT, 7MOU ALT - is
the fallback when there is no order line to read a real partner name from, and
the board now materializes a "needed" override into a row of its own when
nothing else built one.

Approving 53872 now puts Navigate Marketing on the August cycle as an SEO row,
tagged as put on by hand with your note on it. Undo takes it back off.

THOSE ROWS CARRY NO PRODUCTS AND NO DATES, because nothing knows them - the
export is where those come from. They exist so the client is on the cycle and a
report can be uploaded against them, which is what approving one is asking for.

Windber, Jay Gerber and WarePak are the same shape and will behave the same
way. Your four existing approvals were recorded, so they should take effect on
this deploy without you re-doing them.


SOUTHEASTERN COOLING - THE LIFETIME IS OWED
===========================================
51648 ran to 31 August, 51649 starts 1 September. They do not overlap, so the
rule you confirmed does not apply and 51648 is owed a lifetime.

Three places were reading the rolled-up row instead of the orders inside it. A
row is one client and one product across every order carrying it, and its dates
are the WIDEST of them - so this client's row ran May to the end of September:

  * whether a lifetime was owed at all asked the row's end date, 30 September,
    outside the window, so no lifetime row was ever built
  * which order it would be ABOUT took the biggest end date - 51649, which is
    not owed one
  * the overlap test got one window spanning both orders and compared it
    against itself

All three read per-order now. NORTH BAY TIP still waits, which is the rule you
picked, and the reason names 55433 rather than a merged pair of ids.


THE TWO SMALL ONES
==================
"ONLY LISTS CANCELLED WHEN THERE IS ONE LIVE" was my own filter - I dropped any
line status matching the order's to save a repetition, so an IO Live order with
one cancelled line and one live one showed "line items: Cancelled". Every
distinct status is shown now; the only list still suppressed is a single line
item whose status is the order's own.

"IT SPLIT THE CLIENT NAME". "Family Centered Resources, Inc." arrives from a
CSV as two cells and the half with the id is "Inc. #51944 LIFETIME". The rule
that glues the front half back counted LIFETIME as part of the name - eleven
letters, over the threshold - so it never fired. The markers are stripped
before that count now.

803 tests pass.
