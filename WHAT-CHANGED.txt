build 2026.09.08-220
Changed files only. Everything since 130 - install this one.
35 checks.

A spend product is read off its own tiles. Its page is titled "Spend
Performance", which names no product, so the only place PPC appears in words on
Mad Hatter Chimney Cleaning is "PPC Ad Cost" and "PPC Cost-Per-Click" - and
neither was being read. Same shape as LinkedIn.

The same client spelled two ways is one report, not two. Name first, then the
order ids, and the ids only when they name exactly one report. Requiring both
to agree is what was making the duplicate files.

A row shows the copy that was signed off, then the newest, and says "+1 file"
when another report this cycle names the same client.

A completion grid is not more rows of the grid above it.

A row is read one number per column, wherever the table narrows.

A replacement takes the new file's logo.

935 tests pass.
