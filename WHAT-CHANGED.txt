build 2026.09.14-258
Changed files only. Everything since 130 - install this one.
38 checks.

IT SAVED. Your two screenshots say so:

  before   1416 reports behind the current rules. 21 checks switched off.
  after    1417 reports behind the current rules.

The "21 checks switched off" sentence is gone, and every button in the last
column now reads On. That button shows the STATE, not the action - On means
the check is running - which is why it looked like nothing happened when it
had in fact just done what you asked.

SELECT ALL. A button beside the filter, ticking only the rows on screen so it
follows "All" or "Flagging now" rather than quietly selecting the ones you
cannot see. It says Clear once something is ticked.

It had to live OUTSIDE the bulk bar. Everything in there is switched off until
a box is ticked, which is the right rule for the two buttons that act on a
selection and exactly wrong for the one that makes it - it would have needed a
tick before it could tick anything.

Driven in a real browser rather than eyeballed: 38 boxes, Select all ticks 38
and no hidden ones, the label toggles, and "Switch on ticked" leaves nothing
switched off.

1024 tests pass.
