build 2026.09.14-254
Changed files only. Everything since 130 - install this one.
38 checks.

THE GEO-FRAMING ONE. Re-reading the orders fixed the ORDERS and never the
REPORTS, and that is the last reason it kept not appearing.

Findings are stored. A report whose product check abstained while the orders
were stale keeps saying "skipped" until something reads it again - and the
existing re-stamp only queues clients whose PRODUCTS moved. Susquehanna River
Valley's did not: the mapping version changed, its products did not, so it was
never queued, through four builds that each fixed a different reason for it.

A successful order re-read now queues every report whose product check stood
down for stale orders, read off what the report itself recorded. So does the
Re-read the orders button. After that a Run picks them up like anything else.

AND THE BOARD SAYS SO. While the orders are stale, check_products abstains on
EVERY report - and the only sign of it was a tooltip on a toolbar button and a
line inside the "n skipped" expander of one report. There is a banner on the
cycle page now, with the button on it.

So: install, press Re-read the orders, then Run. If the banner is not there
after the re-read, the orders are current and the geo-framing finding will be
on Susquehanna's August report after the Run.

A CTV COMPLETION RATE OF 0.23% IS NOT A COMPLETION RATE. It is a click-through
rate in the completion tile. That is settled by the tile on its own, so it no
longer matters whether the report carries a CTV grid to measure against, and
those reports stop reading "CTV VCR could not be checked" - which was the tool
declining to say the one thing it knew.

  CTV VCR is not a completion rate       p1 · CTV Completion Rate
  The tile reads 0.34%. A completion rate is the share of viewers who watched
  an ad to the end, and no video product finishes two ads in a thousand.

Checked against every CTV fixture on hand - they run 71% to 99% and none of
them is flagged.

--- also in this install ---

The pacing panel is one line per product, with a calendar mark beside the name
on every row that has a day count. "19,463 not paced" is off the panel.

1022 tests pass.
