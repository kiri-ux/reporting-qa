build 2026.08.31-121
Changed files only.

app/checks/products.py
app/main.py
app/orders_io.py
app/roster.py
app/templates/orders.html
app/version.py
tests/test_board_extras.py


IT IS IN THE FILE. THE IMPORT THREW IT AWAY, AGAIN, FOR A DIFFERENT REASON.
==============================================================================
I ran your actual export - 460,784 rows - through the importer. Order 54568 is
in there, 19 rows of it, IO Live, Growth by Design, Credit Union Audit Group.

  product: LinkedIn Ads

LINKEDIN WAS NOT IN THE PRODUCT MAP. Not a spelling it did not recognise - no
entry at all. Every LinkedIn line item in your board has been dropped on import
as "unmapped product", every time, since the beginning. Credit Union Audit
Group sells LinkedIn and nothing else, so the client was not on the board at
all, delivering 31 days a month, with nothing anywhere saying why.

The tool knows about LinkedIn everywhere else, which is what makes this
embarrassing rather than debatable: the export carries monthly_linkedin_ad_
spend and reads it, and the device check excludes LinkedIn by name. The map is
the one place it was missing.

Fixed. "LinkedIn Ads", "LinkedIn", "LinkedIn Display & Video Ads" all map to
LinkedIn now.


AND THE REAL BUG UNDER IT
============================
A product name the map had never seen DELETED THE CLIENT. Not the line, not the
product - the whole client, out of the order list, off the board, no row and no
reason.

That is a dictionary gap taking a live client off the board, silently. It is
the worst kind of failure this tool has: a thing it cannot see, saying nothing.

The line is kept now, under the name the export gave it. Your export has two
more of these:

  Website Visitor ID    116 line items
  Additional Billing     30 line items

Those clients are back on the board with their real dates and money. What does
NOT happen is a report being failed for not carrying "Website Visitor ID" -
nothing knows what that looks like on a page, so the product checks leave it
alone rather than blaming somebody for a gap in the tool's own dictionary.

And the Order list page now names every unmapped product with a count, because
kept quietly is how a product stays unmapped forever. Tell me what those two
should map to - or that they are not reportable products at all - and I will
set them.

Loading your export before and after:

  before   2,291 order lines, 1,192 clients
  after    2,427 order lines, 1,217 clients

25 clients and 136 line items were being thrown away.


ONE MORE THING, FOR THE THIRD TIME
=====================================
The panel was rendering correctly and my test said it was not, because the
sentence it matched on wrapped across two lines in the template - so the phrase
is not in the HTML as one string. That has now cost two rounds of hunting for a
bug in code that was fine.

There is a test for it now: it holds the phrases other tests assert on and
fails if any of them gets wrapped.

708 tests pass.
