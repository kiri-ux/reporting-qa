build 2026.09.11-250
Changed files only. Everything since 130 - install this one.
38 checks.

THE PRODUCT CHECK HAS BEEN SWITCHED OFF ON EVERY REPORT ON THE BOARD, FOR AS
LONG AS THE STAMP HAS HAD A PERIOD ON IT. That is why the missing Geo-Framing
never appeared, on any build, however many times the parser was fixed.

The order sync writes "<hash>:<period>" - the period is in there so the export
gets re-read when the cycle rolls. Four readers compared that against the bare
"<hash>". They never matched, and nothing said so; they just reported the
orders as stale for ever, and check_products abstains while they are stale.

The same mismatch was re-downloading and re-parsing the whole order export
after every single deploy.

One producer now - version.map_stamp() - and a test that fails if a second one
appears. Two existing tests had been writing the bare hash themselves, which is
why they passed while production did not.

Susquehanna River Valley is a paired fixture now. July, which has the line
item, says nothing. August, which does not, says "Ordered but not on the
report: Geo-Framing Display". With the orders reading stale, the check does not
run at all and says why.

A CONVERSION BREAKOUT IS NOT EVIDENCE THAT A PRODUCT RAN. Charlottesville's
Earthly Cleaning bought Display and Performance Max - four line items, not one
of them CTV - and TapClicks printed "CTV Click Conversion Performance Breakout"
and "CTV View-through Conversion Performance Breakout" on it anyway. Those two
titles were the whole of the evidence, so the report was credited with CTV and
then failed for having no Top CTV Publishers widget: a missing widget for a
product nobody bought, off the back of two widgets that should not be there.

The widgets that mean a product ran are its creative, completion and publisher
breakouts. A real CTV buy also has CTV line items, which are still read, so
nothing that genuinely ran loses its product.

AFTER INSTALLING: re-read the orders once, then run the checks. Both numbers
have been wrong since before this session started.

1012 tests pass.
