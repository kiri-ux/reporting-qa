build 2026.09.01-139
Changed files only. Includes 131 through 138 - install this one, not all nine.

app/audit.py
app/board.py
app/db.py
app/main.py
app/orders_io.py
app/orders_s3.py
app/partners.py
app/templates/audit.html
app/templates/base.html
app/templates/why_slow.html      (new)
app/timing.py                    (new)
app/version.py
tests/test_board_extras.py
tests/test_timing.py             (new)


SEO WAS BEING JUDGED BY A FILE IT CAN NEVER BE IN
=================================================
The serving file is ad delivery - impressions against days. SEO is not served,
so an SEO client is absent from that file every month, for ever. And the rule
that reads absence as "it did not run" then took the whole of Whitley's SEO
list off the board with:

  not in the serving file at all - either it did not run, or the serving file
  spells this client differently to the order export

Fourteen rows of one wrong answer, and the answer blamed the file for being
misspelled. Those reports are pulled by hand and uploaded; the row on the cycle
list is the only thing telling anybody they are owed, and it was being deleted.

ANY SEO ON THE ROW, NOT ONLY SEO. A client running SEO beside a digital product
should be in the serving file on the strength of the digital half - but if they
are not, taking the row off loses the SEO report they are still owed too, and
losing a report is the expensive mistake here. The row stays and somebody looks.

WHAT DID NOT CHANGE: a client the file DOES mention, with two days served, is a
fact, and that rule is still on for everybody including SEO. Absence is the
ambiguous signal; a number is not.


AND THE BIG ONE - IT WAS BLAMING THE EXPORT FOR ORDERS THAT ARE IN IT
=====================================================================
53437 is in your export. Three line items, all IO Paused, all ended 30 June.
54338 is in it too, complete. The tool said "it is not in the export, or the
export is out of date" about both.

Here is why, and it explains most of what you have shown me today. The import
keeps the line items that touch the cycle and throws the rest away. The list
check then looked at what survived, found nothing, and concluded the export had
never heard of the order. An empty table read as an empty feed.

That is the worst shape a wrong answer can take here, because it accuses
somebody else's system and sends whoever is on the page to go and check a file
that is perfectly correct. You have now done that several times today.

The import already writes down why it drops each client's rows - 2,337 of them
on your Aug 31 file - and only the lookup page ever read it. The list check
asks now. Against your real export those rows read:

  RegistryAZ           the export has this order and every line on it ended
                       2026-06-30, before August 2026
  Dive Into Space      the export has this order and every line on it ended
                       2026-07-31, before August 2026
  Buffalo Wings        the only lines on this order that reach this cycle are
                       canceled. The other one is not canceled - it ended
                       2026-07-31, before August 2026

"Not in the export" is now only said when nothing was recorded about the client
at all - which is what it always meant.

THE NOTE THAT DID NOT MAKE SENSE was mine, and you were right. I had bolted the
drop reason onto the end with a dash and it read as two half-sentences that had
never met. Every reason is written as a predicate now so the sentence around it
holds together, and there is a test that fails if a new one is added that does
not. Dates read as August 2026 rather than 2026-08.


STILL OUTSTANDING: AUDIOGO AND BROWN BAG NY
===========================================
Those two are still "not in the export", and against the 148 MB file I have
that is all I can honestly say - neither string appears in it anywhere. You can
see them in the 830 MB file. Deploy this, let the sync run, and look at the
sync line: it now names every file it read with its size. If it lists the
830 MB one and those rows still say "not in the export", send me that line and
it is a real bug rather than my copy being half an export.


FROM 136 AND 137
================
The sync was merging every export ever dropped in that S3 folder into today's -
gigabytes an hour of downloading and parsing, and a board that could be half
today and half a fortnight back. It reads the newest run and anything within
twelve hours of it now, which keeps 07:32 and 07:34 together and drops
yesterday. It says which files it read, with sizes, and how many older ones it
walked past. And it checks the disk has room before it starts, rather than
dying halfway through with an error about the file.

778 tests pass.
