build 2026.09.08-206
Changed files only. Everything since 130 - install this one.
35 checks.

Bair Foundation's five "Row CTR does not match" findings are gone. That row
sits at a page break, pdftotext gives it narrow gaps, its first number drops
and the rest shift one column left. A row missing a column the rest of the
table fills in is no longer judged.

The pacing panel's flat-impressions line is "218,084 not paced", list on hover.

Performance Max and PPC are out of the impressions total and the
device-eligible total.

Part-month lines are paced against a part-month goal.

The sweep only waits for quiet traffic on a long deploy-driven queue.

Amazing Results LLC, Ampersand, Grace Impressions: no default-logo finding.

An upload can only replace a report on its own market.

902 tests pass.
