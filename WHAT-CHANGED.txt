build 2026.09.02-175
Changed files only. Everything since 130 - install this one.

Dockerfile
app/audit.py
app/board.py
app/checks/parser.py
app/checks/products.py
app/checks/rules.py
app/checks/served.py
app/checks/spend.py
app/config.py
app/db.py
app/main.py
app/orders_io.py
app/recheck.py
app/roster.py
app/templates/audit.html
app/templates/base.html
app/templates/cycle.html
app/templates/order_lines.html   (new)
app/templates/orders.html
app/templates/report_orders_body.html
app/templates/viewer.html
app/templates/why_slow.html
app/timing.py
app/version.py
tests/fixtures/mcnutt_site_services.pdf   (new)
tests/test_board_extras.py
tests/test_order_remap.py
tests/test_rules.py
tests/test_timing.py


BOTH OF TAYLOR'S - AND HE DIAGNOSED THE FIRST ONE HIMSELF
==========================================================

PAGE 2 LOST THE REPORTER FILTER
===============================
This is the critical one and he is right about what it does: the filter is not
being ignored, the link he clicked never carried it.

Every pager and chip on the board was built from a hand-written list of query
parameters - period, group, state, search, bucket - and the CARD filters
(Partner, Buyer, Reporter, Trainer, Status) were not on that list. So page 2 is
a link to a board with no reporter set, and the dropdown honestly reads "All"
when you land, because that is what the address says.

There is one place now that writes out every filter that is on, and every
pager, bucket and drop-this-filter chip is built from it. Anything added later
is carried by all of them or by none, rather than by whichever ones somebody
remembered.


SIGNING OFF SENT HIM TO ANOTHER PARTNER
=======================================
He worked this out exactly right: it goes back to the last market where he used
View PDF.

Opening a report writes down the board you came from, so that signing off
inside the viewer returns you there rather than leaving you on the PDF. That
cookie was also being used when you sign off from a partner's LIST, where it is
simply the wrong answer - it sends you to wherever you last opened a PDF. Press
the tick in 7 Mountains PA Stroudsburg, land in 7 Mountains PA Dubois. Every
time, as he says.

It goes back to the page the press came from now. The cookie is still the
fallback and still wins when the press came from inside the viewer, which is
the one place it must not land.


843 tests pass.
