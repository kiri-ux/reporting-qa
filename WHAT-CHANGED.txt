build 2026.09.01-164
Changed files only. Everything since 130 - install this one.

app/audit.py
app/board.py
app/checks/products.py
app/checks/rules.py
app/checks/served.py
app/db.py
app/main.py
app/orders_io.py
app/roster.py
app/templates/audit.html
app/templates/base.html
app/templates/cycle.html
app/templates/report_orders_body.html
app/templates/viewer.html
app/version.py
tests/test_board_extras.py
tests/test_order_remap.py
tests/test_rules.py


ADDED BY HAND SAID 1 OF 13
==========================
The table is fifty rows a page. I built that filter in the browser, over the
rows that had been rendered - so it found the one hand-added row on page one
and reported it as the total, with a straight face.

That is exactly the trap the search box was in before, and it has the same
answer: the server applies it, over the whole cycle. The chip is a link now,
the count is off all 1,247 rows, and pressing it shows all thirteen.

I should have caught this. The pagination is the reason the search moved to the
server in the first place, and I put a new filter back on the wrong side of it.


THE PRODUCT PICKER
==================
Chips you click once, instead of a scrolling box that needs a modifier key held
down to pick a second thing - which is the normal case here, not the exception.
They wrap onto two lines and the picked ones fill in.


  Client:  Exactly as listed on RZ


825 tests pass. The orders re-read themselves on the next sync and the reports
re-check in the background - both fingerprints moved.
