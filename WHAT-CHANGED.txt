build 2026.09.01-135
Changed files only. Includes 131 through 134 - install this one, not all five.

app/audit.py
app/db.py
app/main.py
app/orders_io.py
app/partners.py
app/templates/audit.html
app/templates/base.html
app/templates/why_slow.html      (new)
app/timing.py                    (new)
app/version.py
tests/test_board_extras.py
tests/test_timing.py             (new)


BROWN BAG NY AND AUDIOGO - THE TOOL IS RIGHT, AND THAT IS THE BAD NEWS
======================================================================
I searched your Aug 31 export for both. 460,784 rows.

  49943 / Brown Bag NY   zero rows. The string "Brown Bag" is not in the file.
  52029 / AudioGo        zero rows. Neither is "AudioGo", or "ROI SAM".

Neither order is in the export under any status. So it is not Live Chat being
filtered and it is not paused orders being dropped - the tool has never seen
these orders at all, and "not in the export" is the honest answer.

I CHECKED BOTH OF THOSE CLAIMS RATHER THAN ASSUMING THEM.

Paused orders are kept. "IO Paused" is in the export 1,000 times at the order
level and 1,465 at the line level, and the import keeps them - a campaign
paused on the 20th delivered for nineteen days and owes a report for them.

Live Chat is kept. 4,218 rows in the export across 170 orders; 57 order lines
land in the August window, and six clients have Live Chat and nothing else.
I ran the audit's explanation over one of those six and it reads "Live Chat
does not earn a report on its own", which is the new message working. It only
fires when the tool has the order - and for Brown Bag it does not have it.

THE DIFFERENCE BETWEEN THE TWO, WHICH MATTERS. VICI is in the export, 123 rows.
So Brown Bag NY is one order missing from a partner that is otherwise fine.
ROI SAM is not in the export at all - not one row, under any client. That is
not a missing order, it is a missing partner, and every client they have is
invisible to the board this cycle and every cycle.


SO THE PAGE SAYS THAT NOW
=========================
When every row on your list carrying one market code is missing, and missing
because the export has never heard of it, the page says so in a panel above the
table instead of leaving you to notice that six unrelated-looking rows all
start with the same three letters:

  A whole partner is missing from the order export.
  Every row on your list from ROI SAM - 6 of them - is missing, and missing
  because the export has no order line for it at all. One order not in the
  export is an order. All of them is the partner.

It needs two rows before it will say it. With one row under a code there is no
way to tell a missing order from a missing partner, and a panel that cries
partner at every missing order is one people stop reading - which costs the
one time it is real.

AND THE PAGE NOW SAYS WHEN THE EXPORT WAS READ. Half the answers on it end with
"or the export is out of date" and there was nothing on the page that said
whether that was even possible. It is in the line under the title.

Neither of these two is out of date, by the way. Both orders are absent from a
file read on the 31st.


FROM 134, IF YOU SKIPPED IT
===========================
THE VSCU ROWS ran one day in August - Aug 31 to Sep 13 - and the board's
seven-day rule left them off, correctly. The list check threw that reason away
and printed the catch-all. Two faults: your tracker writes a one-word market
code ("ADM - ") and the code that strips it wanted two words, and it only ever
looked the board's reason up by name on a page that exists because the two
tools spell clients differently. It uses the order id now. Those rows read
"ran 1 day this month". Rows the board genuinely has no answer for are tagged
"look" so they stop hiding among the forty that are the rules working.

REJECTING A ROW YOU HAD APPROVED did not take it back off the board. The list
said Rejected and the override stayed put. Fixed, and clearing too.

APPROVING WORKS - I built your Roto Rooter case to confirm it rather than
reading the code and telling you what it looked like it did.

767 tests pass.
