fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
jinja2==3.1.5
python-multipart==0.0.20
pydantic-settings==2.7.1
httpx==0.28.1
pillow==11.1.0
python-dateutil==2.9.0.post0
boto3==1.35.92
openpyxl==3.1.5

# delivery - both optional, the zip is built either way
google-api-python-client==2.149.0
google-auth==2.35.0
dropbox==12.0.2

# NOT USED AT RUNTIME. It backs one test: "no module refers to a name that
# does not exist". A sync crashed on NameError: name 'log' is not defined,
# from a logging line added to a code path that only real S3 credentials
# reach - so no test ran it and nothing caught a typo the compiler cannot.
pyflakes==3.2.0
